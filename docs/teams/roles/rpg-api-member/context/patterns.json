{
  "items": [
    {
      "id": "pat-001",
      "summary": "Input/Output types on every function at every layer",
      "details": "Non-negotiable. Every orchestrator method, every repository method, every service method has *Input and *Output types. Never accept positional args. Never return bare types. Even helpers should use this pattern. This enables adding fields without breaking callers and future pagination.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-002",
      "summary": "No proto types past the handler boundary",
      "details": "Handlers import and use pb. types for conversion. Orchestrators speak internal entity types ONLY. No pb. imports in orchestrators, services, repositories, or entity files. If a type needs to flow past the handler, define a local entity struct for it. The encounter orchestrator currently violates this — 39 pb. refs in orchestrator.go and 3 in service.go. These are tracked violations, not approved patterns.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-003",
      "summary": "Test suites with gomock at interface boundaries",
      "details": "Use testify/suite for all test organization. Use go.uber.org/mock (gomock) for mocking at interface boundaries — not mockery. Place mocks in mock/ subdirectory next to the interface, using package name <domain>mock. Regenerate with go:generate.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-004",
      "summary": "Constructor validates config at startup",
      "details": "Every New() or NewX() constructor validates its Config struct. If a required dependency is nil, return an error immediately. Never nil-check at runtime in methods. Fail fast at startup.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-005",
      "summary": "Never return (nil, nil)",
      "details": "Every function returns either a valid non-nil output or an error. Empty results use empty structs or empty slices, not nil. This applies at every layer.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-006",
      "summary": "Auth context extraction at handler, passed explicitly to orchestrator",
      "details": "Handlers call auth.PlayerIDFromContext(ctx) and pass playerID as a field in the orchestrator's input struct. The orchestrator does not access the context for auth. This keeps auth logic in one layer.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-007",
      "summary": "Outside-in development: handler stub → interface → tests → implementation → repository",
      "details": "Start with a gRPC handler returning codes.Unimplemented. Define the service interface and Input/Output types. Write handler tests with mocked service. Implement the orchestrator. Implement the repository last. See docs/how-to/add-handler-method.md.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-008",
      "summary": "Load-then-stream pattern for real-time encounter state",
      "details": "Clients call GetEncounterState to get a full snapshot with LastEventID, then call StreamEncounterEvents with last_event_id to receive only new events. The orchestrator updates LastEventID in EncounterData whenever an event is published. This is the canonical multi-player state sync pattern.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-009",
      "summary": "Game logic belongs in rpg-toolkit, not rpg-api",
      "details": "If code checks weapon properties, calculates modifiers, evaluates conditions, or implements D&D rules — it belongs in rpg-toolkit. rpg-api passes entities to toolkit functions and persists results. The dungeon component is currently violating this boundary and is scheduled for migration.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-010",
      "summary": "make pre-commit before every commit; make ci-check before every push",
      "details": "pre-commit runs: fmt, tidy, fix-eof, lint, test. ci-check runs scripts/ci-checks.sh for CI-specific validations. Never use --no-verify. If pre-commit fails, fix the issue — CI will fail with the same checks.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-coord-space-merge-boundary",
      "summary": "Apply room origin at the merge boundary; cumulative encounter state is dungeon-absolute",
      "added": "2026-05-04",
      "details": "When folding revealed-room data (walls, entities, obstacles, monster placements) into cumulative encounter state, translate from room-local to dungeon-absolute at the producer using dungeon.Module.LocalToAbsolute (or the localToAbsoluteOrLocal helper in orchestrator.go for the single-room start case). Do this at the OpenDoor merge site, not at downstream consumers.\n\nProducers that write to cumulative state and must apply origin:\n- buildRoomLayoutProto (orchestrator.go ~1098): walls in EncounterStateData.rooms[].walls.\n- localToAbsoluteOrLocal at OpenDoor (~3172): monster.Position in MonsterInfo / Entities map.\n- buildCombinedRoomDataAfterOpenDoor (~5836): obstacle.Position in spatial.RoomData.CubeEntities.\n\nConsumers (handlers, event publishers, perception, movement-collision, roomContainingPosition) treat cumulative state as already-absolute and never re-translate. The proto RoomLayout.Origin field is still emitted alongside absolute walls so a client can recover the local frame by subtraction if needed.\n\nIf you add a new producer of cumulative state and skip origin translation, you reproduce issue #493. Enforce this at code review for any new merge site."
    },
    {
      "id": "pat-v2-handler-direct-no-orchestrator",
      "summary": "v1alpha2 encounter has no orchestrator layer — handler talks directly to the toolkit Encounter + Broker",
      "added": "2026-05-08",
      "details": "Architecture Honesty (chapter 1) move shipped in PR #496. Where v1alpha1 has handler → orchestrator → engine, v1alpha2 has handler → toolkit-encounter-SDK directly. The Boundary Rule still holds: game rules live in rpg-toolkit/encounter (Move, Snapshot, PerceptionView). rpg-api becomes a thinner shim — load Data, call verb, save Data, translate events.\n\nWhen adding a v2 RPC: do NOT introduce an orchestrator. Add a method on the Handler in internal/handlers/dnd5e/v2/encounter/handler.go (or a new file in that package). The handler holds *encounter.Broker, encountersv2.Repository, and a now func() time.Time injected via HandlerConfig.\n\nIf you find yourself wanting an orchestrator (\"too much logic in the handler\"), the right move is usually to push the logic into rpg-toolkit/encounter, not to add an orchestrator layer. File a toolkit issue for the missing helper."
    },
    {
      "id": "pat-v2-directory-layout",
      "summary": "v1alpha2 encounter file layout established by slice 1",
      "added": "2026-05-08",
      "details": "PR #496 established this layout. New v2 work fits this shape — don't reinvent it.\n\nHandler (one package per service):\n  internal/handlers/dnd5e/v2/encounter/\n    handler.go        — Handler struct, New(*HandlerConfig), MoveEntity, StreamEncounter\n    handler_test.go   — handler unit tests with mocked Broker/Repo\n    translate.go      — toolkit-event → proto-envelope translator + ErrViewerSawNothing/ErrUnknownEventType\n    translate_test.go — translator unit tests per event type\n\nRepository (in-memory + Redis, both implement encountersv2.Repository):\n  internal/repositories/encounters/v2/\n    repository.go  — Repository interface (Get, Save, ErrNotFound, etc.)\n    in_memory.go   — NewInMemory() — round-trips through ToData/LoadFromData\n    redis.go       — Redis-backed; encoding via toolkit serializer\n    *_test.go      — both implementations covered by the same suite\n\nIntegration tests (bufconn — slice 1 gate test):\n  internal/integration/encounter_v2_test.go — EncounterV2IntegrationSuite\n  internal/integration/harness/harness.go    — extension fields TestServer.{EncounterClientV2, BrokerV2, EncRepoV2}; harness.New seeds + registers v2 service alongside v1alpha1\n\nServer wiring:\n  cmd/server/server.go — registers v2 service alongside v1alpha1; both alive."
    },
    {
      "id": "pat-v2-broker-process-scoped",
      "summary": "v2 Broker is process-scoped on the server struct, in-memory transport for now",
      "added": "2026-05-08",
      "details": "The encounter Broker (rpg-toolkit/encounter.Broker) is constructed once at server startup over tkenc.NewInMemoryTransport() and held on the server struct. The same Broker instance is shared between the handler that publishes (Move, future OpenDoor/Attack) and StreamEncounter that subscribes. There is one Broker per process — encounters are keyed by EncounterID inside the Broker, not via separate Broker instances.\n\nDo not construct a per-request Broker. Do not pass Brokers through the request path. Inject the process-scoped Broker into HandlerConfig at startup.\n\nFuture: Redis transport when we go multi-process. The interface is pluggable but slice 1 doesn't ship it. See rpg-toolkit/encounter docs for transport contract."
    },
    {
      "id": "pat-v2-translator-typed-errors",
      "summary": "v2 translator uses ErrViewerSawNothing / ErrUnknownEventType sentinels; stream loop is errors.Is-aware",
      "added": "2026-05-08",
      "details": "translate.go's TranslateEvent(evt, viewer, now) returns three error shapes the stream loop must handle distinctly:\n\n  - ErrViewerSawNothing: the broker delivered an event to this viewer but the viewer's per-player slice is empty (e.g. MoveEvent's PerPlayer[viewer].SeenSegments is empty). The stream loop SILENTLY continues — do not log, do not send.\n  - ErrUnknownEventType: the translator has no mapping for the concrete event type (a new toolkit event landed that the translator doesn't know yet). The stream loop LOGS via log.Printf with encounter+type, then continues. This surfaces the gap in production.\n  - any other error: a real bug. Stream loop returns codes.Internal.\n\nWhen adding a new toolkit event to the translator: add a case in the TranslateEvent type-switch, write a translate<EventType> helper that handles per-viewer slicing, and add a translate_test.go case. Do NOT swallow ErrViewerSawNothing in the helper — return it to the loop."
    },
    {
      "id": "pat-v2-bufconn-test-pattern",
      "summary": "v2 integration tests use the bufconn harness; encounter is constructed via tkenc.New + harness.EncRepoV2.Save",
      "added": "2026-05-08",
      "details": "The slice-1 gate test (internal/integration/encounter_v2_test.go) is the canonical pattern for v2 integration tests. To add a new test:\n\n  type MyV2TestSuite struct {\n      suite.Suite\n      ctx context.Context\n      srv *harness.TestServer\n  }\n\n  func (s *MyV2TestSuite) SetupTest() {\n      s.ctx = context.Background()\n      var err error\n      s.srv, err = harness.New(s.ctx, nil)\n      s.Require().NoError(err)\n  }\n\n  func (s *MyV2TestSuite) TearDownTest() { s.srv.Close() }\n\nSeed an encounter directly through the toolkit + repo — this avoids needing CreateEncounter to exist:\n\n  enc := tkenc.New(\"enc-1\", s.srv.BrokerV2)\n  // ... configure players, geometry, entities via toolkit verbs\n  s.Require().NoError(s.srv.EncRepoV2.Save(s.ctx, enc.ToData()))\n\nThen exercise via s.srv.EncounterClientV2 (a real gRPC client over bufconn). Stream + RPC calls go through the registered v2 handler, broker projection runs end-to-end. authCtx(playerID) helper attaches the Authorization: Dev <playerID> header.\n\nOnce CreateEncounter exists (rpg-api#499), tests can also create through the RPC — but the direct-construct path stays valid for fixture setup."
    },
    {
      "id": "pat-v2-status-code-mapping",
      "summary": "Toolkit Move errors → FailedPrecondition; empty proposed_path → InvalidArgument",
      "added": "2026-05-08",
      "details": "Settled mid-flight in slice 1 (rpg-api#495 closed by PR #496). Toolkit encounter.Move returns errors for state-dependent failures: turn order mismatch, blocked path, out-of-range, entity-not-in-encounter. These are gRPC FailedPrecondition by convention — the request was syntactically valid but the world state forbids the action.\n\nThe one truly argument-shaped error is empty proposed_path — caught BEFORE Move is called and returned as InvalidArgument.\n\nApply the same convention to future v2 verbs: state-dependent → FailedPrecondition; truly malformed input → InvalidArgument; auth → PermissionDenied; missing entity/encounter → NotFound; auth missing → Unauthenticated.\n\nDocument any new mapping in rpg-api/docs/architecture/components/encounter.md."
    },
    {
      "id": "pat-v2-projectFor-helper",
      "summary": "ProjectFor is the single source of truth for converting (encounter.Data, viewer) → proto Encounter; never inline projection",
      "added": "2026-05-08",
      "details": "Validated in Wave 2.6 (PR #501 extracted, #502 broadened, #503 reused). Located at `internal/handlers/dnd5e/v2/encounter/project.go`. Signature:\n\n  func ProjectFor(data *encounter.Data, viewer core.PlayerID, broker *encounter.Broker, now time.Time) (*encounterv2pb.Encounter, error)\n\nResponsibilities:\n- Calls `encounter.LoadFromData(data, broker)` and `enc.SnapshotFor(viewer)` to get the toolkit Snapshot.\n- Iterates `data.Players` (sorted by player ID for determinism) and includes the viewer plus any non-viewer player whose `View.Position` is within the viewer's current sight range via `perception.VisibleHexesAt(snap.Position, sightRange)`.\n- Translates revealed hexes from the viewer's `View.RevealedHexes` to the proto wire shape.\n\nUsed by: CreateEncounter handler (returns the projection in CreateEncounterResponse.encounter), GetEncounter handler (same), StreamEncounter snapshot replay (feeds both TranslateSnapshot and BuildReplayEvents — see pat-v2-snapshot-replay-builder-pair).\n\nWhen adding a new RPC or stream-side flow that needs a per-viewer snapshot of an encounter, call ProjectFor. Do NOT inline the projection logic. If ProjectFor needs broadening for a future shape (entities beyond players, condition visibility, last-known-position for ghosts), extend ProjectFor — keep the single seam."
    },
    {
      "id": "pat-v2-snapshot-replay-builder-pair",
      "summary": "StreamEncounter on connect emits SnapshotDelivered + synthetic replay events; TranslateSnapshot + BuildReplayEvents share the ProjectFor output",
      "added": "2026-05-08",
      "details": "Pattern shipped in PR #503 (#497). The StreamEncounter handler's connect sequence:\n\n  1. Subscribe(encID, viewer) — buffered channel begins capturing live events\n  2. encRepo.Get + LoadFromData — load + rehydrate\n  3. enc.SnapshotFor(viewer) — toolkit snapshot\n  4. ProjectFor(data, viewer, broker, now) — build proto Encounter ONCE\n  5. TranslateSnapshot(snap, projected, now) — wraps as SnapshotDelivered envelope\n  6. BuildReplayEvents(projected, snap, now) — synthesizes []EntityAppeared + GeometryRevealed\n  7. stream.Send(SnapshotDelivered) + each replay event\n  8. live forward loop drains buffered channel\n\nKey design decisions:\n- ProjectFor runs ONCE; both TranslateSnapshot and BuildReplayEvents consume its `*pbEncounter` output. Neither duplicates projection logic.\n- TranslateSnapshot signature now takes `*pbEncounter` (was untyped before #503's drive-by) so it doesn't need to know about projection.\n- Both helpers exported (capitalized) so external `_test` package can exercise them directly.\n- Subscribe-before-snapshot ordering preserved: live events that fire during steps 2-7 are buffered and delivered after the replay completes. Tests cover this with `TestStreamEncounter_LiveEventsDoNotDuplicateReplay`.\n- 'initial state' string is the Reason on synthetic replay events (file `BuildReplayEvents`); could become a constant if more reason strings accumulate. Two PR reviewers flagged it as deferred-cleanup — file an issue if a third reviewer mentions it.\n\nWhen adding new event types to v2 that need to be present in the initial scene a viewer connects to: extend BuildReplayEvents to synthesize the corresponding initial-state shape from ProjectFor's output."
    },
    {
      "id": "pat-v2-interact-handler-shape",
      "summary": "Interact RPC dispatches to a toolkit verb by target-type discriminator (door/chest/lever/...)",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.7 playtest 2026-05-09",
      "details": "Shipped in Wave 2.7 (rpg-api PR #506). The Interact handler is a thin dispatch:\n\n  1. Auth: pull viewer PlayerID from ctx.\n  2. Load: encRepo.Get(req.encounter_id) + encounter.LoadFromData(data, broker).\n  3. Validate target.case oneof — currently only `door` is implemented; new target types add a case here.\n  4. Dispatch: switch on target.case → call the matching toolkit verb on the encounter aggregate (door → enc.OpenDoor(viewer, doorRef)). The handler does not implement the verb; rules live in rpg-toolkit/encounter.\n  5. Save: encRepo.Save(enc.ToData()).\n  6. Live events flow via the broker on their own — handler does NOT manually publish the outcome events.\n\nWhen adding a new target type to Interact (chest, lever, trap, NPC dialogue start):\n  - Add the toolkit verb on rpg-toolkit/encounter first; let it emit its own cause + effect events.\n  - Add the case in the handler's target-type switch; map proto target ref to toolkit input.\n  - Map toolkit errors via pat-v2-status-code-mapping (state-dependent → FailedPrecondition; missing target → NotFound).\n  - Extend the translator (translate.go) for any new event types the verb publishes.\n\nDo NOT inline the verb's logic in the handler — that's the same orchestrator anti-pattern pat-v2-handler-direct-no-orchestrator warns against, just renamed."
    },
    {
      "id": "pat-v2-cause-effect-event-split",
      "summary": "Translator must NOT combine cause + effect events; toolkit deliberately decouples them and the wire keeps them separate",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.7 + 2.8 playtest 2026-05-09",
      "details": "Validated across Waves 2.7 (DoorOpened/HexRevealed) and 2.8 (AttackResolved/DamageDealt/ConditionApplied). The toolkit publishes cause-stage events (DoorOpenedEvent, AttackResolvedEvent) AND effect-stage events (HexRevealedEvent, DamageDealtEvent, ConditionAppliedEvent) as separate events with separate per-viewer projections. The rpg-api translator MUST NOT collapse a cause + effect into a single wire message.\n\nRationale (from Wave 2.7 design + verified 2.8):\n  - Different viewers may see only one half. Player A sees the door open from inside the room (cause); player B sees the new hexes reveal but didn't perceive the cause (e.g. stealth scenario).\n  - Different consumers care about different halves. Animations hook the cause (\"play attack swing\"), HP UI hooks the effect (\"-5 HP\").\n  - The narrative log uses both as distinct lines (\"goblin attacks alice\" vs \"alice takes 5 damage\").\n\nTranslator rule: one toolkit event → at most one proto wire event. If the toolkit publishes 3 events for a single verb, the wire emits 3 events. Per-viewer slicing happens independently for each.\n\nValidated reality note (2026-05-09): AttackResolvedEvent does NOT reach the wire today. dnd5e action layer uses a stand-in resolver in the encounter SDK instead of routing through combat.ResolveAttack — only DamageDealtEvent + ConditionAppliedEvent fire on the wire for the goblin's attack. Tracked as rpg-toolkit#635. Once #635 lands, the cause-stage AttackResolved will start flowing and the translator's existing AttackResolved case will activate. Until then, design as if it flows — don't strip the case."
    },
    {
      "id": "pat-v2-mode-gating",
      "summary": "TakeAction + EndTurn handlers check enc.Mode() first; return FailedPrecondition if not TURN_BASED",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.8 playtest 2026-05-09",
      "details": "Shipped in Wave 2.8 (rpg-api PR #507). Mode-aware handlers must check the encounter mode before dispatching the verb:\n\n  if enc.Mode() != core.EncounterModeTurnBased {\n      return nil, status.Error(codes.FailedPrecondition, \"encounter is not in TURN_BASED mode\")\n  }\n\nApplies to: TakeAction, EndTurn, and any future RPC whose semantics only make sense in initiative-based combat. MoveEntity stays mode-agnostic (free-roam movement is valid).\n\nWhy FailedPrecondition (not InvalidArgument): the request is syntactically valid; world state forbids it. Same convention as pat-v2-status-code-mapping.\n\nWhen adding a new turn-based-only RPC: include the mode check at the TOP of the handler, before any other validation that could produce a confusing error. Test with both modes (FREE_ROAM + TURN_BASED) in the handler suite — see encounter_v2/take_action_test.go for the template."
    },
    {
      "id": "pat-v2-npc-turn-dispatch",
      "summary": "After EndTurn advances initiative, handler inspects new active actor; if NPC, calls enc.NPCAct and recursively cycles through any subsequent NPC turns",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.8 playtest 2026-05-09",
      "details": "Shipped in Wave 2.8 (rpg-api PR #507). The EndTurn handler doesn't just advance initiative — it drives the NPC cycle until the next player is active:\n\n  enc.EndTurn(viewer)  // advances ActiveIdx, may wrap round\n  for i := 0; i < len(enc.InitiativeOrder()) + 4; i++ {\n      active := enc.ActiveActor()\n      if active is a player → break\n      if active is an NPC → enc.NPCAct(active)  // toolkit publishes the NPC's turn events\n      // loop continues; NPCAct calls EndTurn internally, so the next iteration sees the new active actor\n  }\n\nLoop bound = initiative_size + 4 — defensive cap against an infinite cycle if every actor is somehow an NPC. Designed in the Wave 2.8 plan; verified at playtest as one client RPC producing 5 events in a single response: `EndTurn(bob) → TurnEnded(bob) → TurnStarted(goblin) → EntityDamaged(alice) → TurnEnded(goblin) → TurnStarted(alice round=2)`.\n\nThe NPC's turn events fire from inside the toolkit (via enc.NPCAct → monster.TakeTurn → bus → broker), so the handler does NOT manually publish them. The handler's only responsibility is the cycle. Keep the cap loose enough for many-NPC encounters but tight enough to fail loudly on a real bug.\n\nFuture: when reactions / opportunity attacks land, this loop becomes more involved (reactions interleave between turns). For now, simple cycle-until-player is sufficient."
    },
    {
      "id": "pat-v2-combat-event-translator",
      "summary": "Combat events: DamageDealt → EntityDamaged; ConditionApplied → StatusApplied; mode/turn events → corresponding proto events; AttackResolved drops on the floor today (#635)",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.8 playtest 2026-05-09",
      "details": "Shipped in Wave 2.8 (rpg-api PR #507). The translator's TranslateEvent type-switch added cases for the Wave 2.8 toolkit events:\n\n  - DamageDealtEvent → encounterv2pb.EntityDamaged (target id, amount, hp_after.current/max)\n  - ConditionAppliedEvent → encounterv2pb.StatusApplied (target id, condition ref, source ref, duration)\n  - ModeChangedEvent → encounterv2pb.ModeChanged (new mode)\n  - TurnStartedEvent → encounterv2pb.TurnStarted (active entity id, round)\n  - TurnEndedEvent → encounterv2pb.TurnEnded (entity id that just ended)\n  - AttackResolvedEvent → encounterv2pb.AttackResolved (cause-stage; not currently on wire — see below)\n\nAll cases honor pat-v2-translator-typed-errors: ErrViewerSawNothing for empty per-viewer slices, ErrUnknownEventType for unrecognized types.\n\nValidated reality note (2026-05-09): AttackResolvedEvent does NOT fire on the wire today. The dnd5e action layer (bite, scimitar, melee, ranged) only publishes the cause AttackEvent without resolution data; the encounter SDK uses a stand-in resolver to compute hit/damage from monster snapshot fields instead of routing through combat.ResolveAttack. Tracked as rpg-toolkit#635. The translator's AttackResolved case is wired and tested — it just doesn't get exercised yet. Once #635 lands, AttackResolved will start flowing and animation-layer consumers can subscribe to it.\n\nWhen adding new combat event types: extend TranslateEvent + add a translateCombatXxx helper that handles per-viewer slicing + add a translate_combat_test.go case. Keep cause and effect as separate cases — see pat-v2-cause-effect-event-split."
    },
    {
      "id": "pat-v2-projection-and-events-must-stay-in-sync",
      "summary": "Every wave that adds fields to encounter.Data MUST update ProjectFor in the same PR; live events and snapshot projection are TWO paths",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.8 playtest 2026-05-09 (discovered via rpg-api#509-#512)",
      "details": "Discovered during Wave 2.8 playtest 2026-05-09. The combat handlers in PR #506/#507 wired LIVE event flow correctly — taking actions and rolling initiative produced the right wire events. But the SNAPSHOT path on initial connect / reconnect / refresh sent an Encounter proto missing the Wave 2.8 fields:\n\n  - data.Mode → projection hardcoded ENCOUNTER_MODE_FREE_ROAM\n  - data.Initiative + ActiveIdx + Round → no TurnState built\n  - data.Monsters → only player entities iterated\n  - PlayerData → entities lacked type + HP + CharacterData oneof\n\nResult: clients that joined late or refreshed mid-combat saw mode=UNSPECIFIED. The web combat panel gates on `mode === 'TURN_BASED'`, so the harness combat section never enabled — even though the live events were correct. Fixed across rpg-api#509 (mode + turn_state + monsters), #510 (player type/HP/CharacterData), #511/#512 (player projection).\n\nThe lesson: ProjectFor in `internal/handlers/dnd5e/v2/encounter/project.go` is the second path that consumes encounter.Data. Live event handlers update Data + emit events; snapshot projection reads Data and emits a single Encounter proto. Forgetting to update ProjectFor surfaces only when a viewer connects fresh — silent for the lifetime of any session that started from an empty encounter.\n\nWave checklist whenever encounter.Data grows new fields:\n  1. Toolkit publishes new events → translator gets new cases (pat-v2-combat-event-translator).\n  2. Toolkit aggregate persists new state on Data → ProjectFor MUST consume that state and emit it on the wire shape.\n  3. Both paths covered by test fixtures: handler suite for live events, project_test.go for snapshot.\n  4. Manual seed-and-refresh playtest confirms a fresh client sees the same world a live client sees.\n\nIf a wave plan describes both new events AND new Data fields, the close-the-loop reviewer should sanity-check both halves landed. Reference: rpg-api#509 issue body for the proof and fix sketch."
    },
    {
      "id": "pat-v2-prompt-on-response",
      "summary": "Caller-private prompts ride on InteractResponse.input_required / TakeActionResponse.input_required, never as a stream event",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.9 playtest 2026-05-09",
      "details": "Shipped in Wave 2.9 (rpg-api PR #515). Prompts (skill check / dialogue / target select) are caller-private game state — only the player who triggered the prompt should see the DC, ability, and tool requirements. They flow on the unary RPC response (`InteractResponse.input_required` field 2; `TakeActionResponse.input_required` field 2), NOT through the encounter Broker.\n\nThe contract has carried `InputRequired` / `SkillCheckPrompt` since Phase 1 (`types.proto:284-317`); Wave 2.9 was the first verb to populate it. The Interact handler, after detecting a locked-door target, builds an `InputRequired{skill_check: {dc, ability, tool}}` and sets it on the InteractResponse — and emits zero stream events for the prompt itself. The world-affecting outcome (door opens or stays closed) flows through the Broker once SubmitCheck resolves the prompt.\n\nWhy never on the stream:\n  - Other viewers should NOT learn the DC / ability / tool of another player's pending check (info leak about narrative state).\n  - The Broker fan-out delivers to all subscribers; per-viewer slicing exists for outcome events but is the wrong shape for a single-recipient prompt.\n  - The caller already has the response in hand to drive UI; no second round-trip needed.\n\nWhen adding a future prompt-issuing RPC (e.g., `BeginDialogue`): set `input_required` on its response, do NOT publish a `PromptIssued` event. The outcome of the prompt's resolution flows on the stream as normal world events."
    },
    {
      "id": "pat-v2-submit-check-handler",
      "summary": "SubmitCheck handler delegates entirely to the toolkit Encounter aggregate; no rules logic in rpg-api",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.9 playtest 2026-05-09",
      "details": "Shipped in Wave 2.9 (rpg-api PR #515). `internal/handlers/dnd5e/v2/encounter/submit_check.go` is a thin shim over `enc.SubmitCheck(playerID, roll)`. The handler:\n\n  1. Auth → `auth.GetPlayerID(ctx)`; empty → Unauthenticated.\n  2. Validate request shape: empty `EncounterId` / `EntityId` → InvalidArgument; roll outside [1,20] → InvalidArgument.\n  3. Load encounter via `encRepo.Get` + `encounter.LoadFromData(data, broker, encounter.WithCharacterResolver(h.resolver))`.\n  4. Call `enc.SubmitCheck(playerID, roll)` — toolkit verb owns: pending-prompt lookup, ability-modifier math, tool-proficiency bonus, total computation, success comparison, dispatch to `triggeredAction` (e.g. `OpenDoor`), pending-prompt clearing.\n  5. Map toolkit errors via pat-v2-status-code-mapping: `ErrNoPendingPrompt` → FailedPrecondition; missing target → NotFound; etc.\n  6. Save encounter via `encRepo.Save(enc.ToData())`.\n  7. Return `SubmitCheckResponse{success, total}`.\n\nEvents (DoorOpened + HexRevealed on success) fire from inside the toolkit during `enc.SubmitCheck`; the handler does NOT manually publish them. Same convention as the other v2 verbs (pat-v2-handler-direct-no-orchestrator, pat-v2-interact-handler-shape).\n\nWhen extending SubmitCheck to new prompt kinds (DialogueChoice, TargetSelect): add the dispatch case inside the toolkit's `dispatchPromptAction`, not the handler. The handler stays thin — only auth, validate, load, call, save, map errors, return."
    },
    {
      "id": "pat-v2-pending-prompt-collision",
      "summary": "Interact (and any future prompt-issuing RPC) returns FailedPrecondition if the caller already has a pending prompt; server enforces 'at most one pending'",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.9 playtest 2026-05-09",
      "details": "Shipped in Wave 2.9 (rpg-api PR #515). The pending-prompt invariant from `design.md §4` (\"a player can have at most one pending prompt at a time\") is enforced server-side. When the caller invokes Interact while `encounter.Data.PendingPrompts[playerID]` is non-nil, the toolkit returns `ErrPromptAlreadyPending` and the handler maps it to `codes.FailedPrecondition` (\"resolve pending prompt first\"). Per feedback_no_logic_in_web, the web does NOT gate on pending-prompt state — it just calls the RPC and surfaces the error.\n\nThis collision rule applies to:\n  - Interact on a locked door while another check is pending (e.g., player triggered a dialogue prompt from an NPC and now tries to pick a lock without resolving).\n  - Future prompt-issuing RPCs (BeginDialogue, etc.) follow the same rule.\n\nMoveEntity, EndTurn, TakeAction (non-prompt-issuing actions) are NOT gated by pending-prompt state in Wave 2.9 — their semantics don't conflict with an outstanding skill check. If a future wave decides movement should pause during prompts, that's an explicit gate addition, not a default.\n\nResolution path: caller submits SubmitCheck (success OR failure clears the prompt — see pat-v2-thread-resolver-everywhere reality note in toolkit-member's pat-encounter-pending-prompt) and may then re-issue Interact.\n\nWhen wiring a new prompt-issuing RPC: copy the pre-flight check from interact.go's locked-door branch — load encounter, check `data.PendingPrompts[playerID]`, return FailedPrecondition early if non-nil. Don't try to enqueue / queue prompts; the design choice is single-slot, fail-fast."
    },
    {
      "id": "pat-v2-thread-resolver-everywhere",
      "summary": "WithCharacterResolver(h.resolver) MUST thread through every LoadFromData / tkenc.New call site so SubmitCheck can run modifier math regardless of which prior verb created the encounter state",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.9 playtest 2026-05-09 (discovery story rpg-api#515)",
      "details": "Discovered during Wave 2.9 implementation (rpg-api#515). The toolkit's `SubmitCheck` verb computes `roll + ability_modifier(playerID, prompt.ability) + tool_proficiency_bonus(playerID, prompt.tool)` — both lookups go through the `CharacterResolver` interface that `encounter.WithCharacterResolver(...)` wires in at LoadFromData / tkenc.New time. If any prior verb in the encounter's lifetime called `encounter.LoadFromData(data, broker)` WITHOUT the resolver option, the encounter's runtime aggregate has no resolver attached. SubmitCheck against that encounter then returns `ErrNoCharacterResolver` even though the resolver is sitting on the handler struct.\n\nThe fix: every handler that constructs or rehydrates an encounter must thread `encounter.WithCharacterResolver(h.resolver)`:\n\n  - CreateEncounter (`tkenc.New(id, broker, encounter.WithCharacterResolver(h.resolver))`)\n  - MoveEntity\n  - EndTurn\n  - TakeAction\n  - Interact\n  - SubmitCheck\n\n`ProjectFor` (read-only — calls `LoadFromData` then `SnapshotFor`) is the one exception that's safe to skip: it never calls a verb that needs modifier math. But threading the option there too is harmless and keeps the call-site shape uniform.\n\nFailure mode is silent until SubmitCheck runs against an encounter created by a non-resolver-wired path — e.g., player creates an encounter, walks around, opens the locked door's prompt, submits a roll → `ErrNoCharacterResolver`. The other verbs work fine without the resolver because they don't touch character state.\n\nReference rpg-api#515 for the discovery story. When adding a new v2 verb that constructs/rehydrates an encounter: include `encounter.WithCharacterResolver(h.resolver)` in the option list at the call site, even if the verb itself doesn't need it. Future verbs that DO need it will inherit the wired runtime."
    },
    {
      "id": "pat-wall-consumer-scope-revealed-union",
      "summary": "Wall consumers load the union of all revealed rooms' walls in dungeon-absolute, not just currentRoom.Walls",
      "added": "2026-05-05",
      "details": "Issue #465 (wall-consumer scope, separate from the type-unification scope tracked under the same issue number). Before this rule, every wall-aware path in the encounter orchestrator wrote a snippet like:\n\n  if currentRoom := dungeon.GetRoom(currentRoomID); currentRoom != nil {\n      dungeonWalls = currentRoom.Walls\n  }\n\nThat is wrong any time more than one room is revealed. Once the player walks through OpenDoor, every other revealed room's perimeter is invisible to collision and to the wall set published in events.\n\nThe rule: use loadAbsoluteWallsForRevealedRooms(dng) in revealed_walls.go. It iterates dng.RevealedRooms, applies each room's origin (from dng.RoomOrigins) to every wall vertex element-wise, and returns the union in dungeon-absolute. The validator PathCrossesWall (and wallsToBlockedHexes for monster perception) is a 2D-segment-intersection / cube-rasterization function — frame-agnostic, so absolute-on-absolute works.\n\nConsumers that must use the union:\n- MoveCharacter (orchestrator.go ~1620): wall-collision check.\n- executeMoveAction (orchestrator.go ~5290): wall-collision per path step.\n- executeMonsterTurns (monster_turns.go ~50): perception BlockedHexes + validateMovementAgainstWalls.\n- ResolveAttack (orchestrator.go ~520): AttackResolved event Walls field.\n- EndTurn (orchestrator.go ~2065): TurnEnded / MonsterTurnCompleted event Walls field.\n\nDoor positions are passable by construction — perimeter generation (internal/components/dungeon/toolkit/perimeter.go) splits walls AROUND the door hex via createWallBeforeDoor / createWallAfterDoor, so the door is a gap in the wall list. No special-case logic needed in the collision check.\n\nIf you write a new consumer that calls dungeon.GetRoom(currentRoomID).Walls and treats that as the encounter wall set, you reproduce #465. Enforce this at code review."
    },
    {
      "id": "pat-v2-encounter-scoped-resolver",
      "summary": "Dnd5eCombatResolver uses enc.EventBus() (encounter-scoped, persistent) — never events.NewEventBus() per attack — and layers gamectx.WithGameContext / WithRoom / WithReactionReadiness on every call",
      "added": "2026-05-14",
      "status": "validated",
      "validated_in": "Wave 2.11c (rpg-api PR #532)",
      "details": "Shipped in Wave 2.11c (rpg-api PR #532). The rpg-api combat resolver `internal/handlers/dnd5e/v2/encounter/dnd5e_combat_resolver.go`:\n\n  1. Retrieves the encounter-scoped persistent bus: `bus := enc.EventBus()`. NEVER `events.NewEventBus()` per call — that breaks any condition with a TurnEndTopic subscription (Sneak Attack's UsedThisTurn reset, etc.). See toolkit-member's pat-encounter-scoped-bus for why.\n  2. Builds the broadened CharacterRegistry via `buildEncounterCharacterRegistryFromResolved` — every combatant in the encounter, not just attacker+target. Protection-style conditions need to query allies; narrow registries fail lookup.\n  3. Layers gamectx via:\n     - `gamectx.WithGameContext(ctx, registry)` — attaches the CharacterRegistry.\n     - `gamectx.WithRoom(ctx, hexRoomAdapter)` — attaches the spatial Room (see hex_room.go for the adapter).\n     - `gamectx.WithReactionReadiness(ctx, enc.ReactionReadiness())` — attaches the per-character readiness map (Wave 2.11d will use this for opt-in player reactions).\n  4. Calls `combat.ResolveAttack(ctx, input)` (or, post-Wave-2.11d, `ResolveAttackHit` then orchestrate phase 2 — see pat-v2-discrete-attack-orchestration when that lands).\n  5. After the call, runs `saveAttackerConditionState(ctx, enc, attackerID)` — rehydrates the character, calls cond.ToJSON() on each condition, writes the new JSON blobs back to character.Data.Conditions, saves the character. Without this write-back, condition state changes during the attack (UsedThisTurn flipping true) get discarded; the next RPC reloads the OLD JSON.\n\nWhy write-back is required even with persistent bus: conditions are re-Apply()'d from JSON each handler call (the runtime Condition struct doesn't survive across RPCs — only the JSON blob does). The bus persistence keeps the SUBSCRIPTION alive within a single in-memory lifetime, but state mutations in the runtime struct only persist if you ToJSON() and write back.\n\nWhen wiring a new combat-adjacent handler (counterspell, opportunity attack response in Wave 2.11d): use `enc.EventBus()`, build the broadened registry, layer all three gamectx pieces, run the toolkit verb, write back any condition state that mutated. Don't skip the write-back even for verbs that 'shouldn't' mutate state — defensive write-back catches bugs the once-per-turn-style traps create."
    },
    {
      "id": "pat-v2-end-turn-publish-and-persist",
      "summary": "EndTurn handler uses publishTurnEndAndPersistReset — load char → rehydrate with bus → fire TurnEndEvent → ToJSON → save char back; without write-back, once-per-turn condition resets are silently discarded",
      "added": "2026-05-14",
      "status": "validated",
      "validated_in": "Wave 2.11c (rpg-api PR #532)",
      "details": "Shipped in Wave 2.11c (rpg-api PR #532). The EndTurn handler at `internal/handlers/dnd5e/v2/encounter/end_turn.go` uses a `publishTurnEndAndPersistReset` helper for every character whose turn just ended:\n\n  1. Load the character from the character repo.\n  2. Rehydrate with the encounter-scoped bus: `character.LoadFromData(ctx, &charData, enc.EventBus())`. This re-Apply()s every condition; any TurnEnd subscriber is now wired.\n  3. Publish TurnEndEvent on the bus. Conditions like SneakAttackCondition handle this synchronously: their onTurnEnd flips runtime state (UsedThisTurn → false).\n  4. ToJSON each condition, write the new blobs back to charData.Conditions.\n  5. Save the character.\n\nThis matches the same write-back pattern as pat-v2-encounter-scoped-resolver (saveAttackerConditionState after each attack). The reason: Apply() during step 2 creates fresh runtime structs from JSON; the JSON has the OLD UsedThisTurn=true; without write-back the next RPC reloads the same OLD JSON and the reset is lost.\n\nFootgun: an earlier (broken) implementation skipped step 4-5, assuming TurnEndEvent's listeners would persist their own state. They can't — the bus is encounter-scoped but the character's persistence path goes through the character repo, not the encounter. The rpg-api side owns the write-back boundary because it owns the storage boundary.\n\nWhen adding a new turn-boundary event (StartOfTurn, EndOfRound): same shape — load, rehydrate, fire, ToJSON, save. Generalize the helper if more than two callers emerge."
    },
    {
      "id": "pat-v2-spatial-hex-room-adapter",
      "summary": "hex_room.go adapts encounter Data → spatial.Room interface so gamectx.WithRoom can hand chain handlers the standard Room contract; mutating verbs return errors (read-only adapter)",
      "added": "2026-05-14",
      "status": "validated",
      "validated_in": "Wave 2.11c (rpg-api PR #532)",
      "details": "Shipped in Wave 2.11c (rpg-api PR #532). The combat resolver needs to give gamectx.WithRoom a `spatial.Room` so chain handlers (Sneak Attack adjacency check, Protection ally-within-5ft check) can call `room.GetEntityPosition`, `room.GetEntitiesInRange`, etc.\n\nrpg-api's encounter Data isn't a spatial.Room — it's the encounter aggregate. The adapter at `internal/handlers/dnd5e/v2/encounter/hex_room.go` wraps the encounter to satisfy the spatial.Room interface:\n\n  - GetEntityPosition(entityID) — looks up in encounter.Data.Entities + encounter.Data.Monsters.\n  - GetEntitiesInRange(center, radius) — iterates entities, filters by hex distance.\n  - Mutating verbs (PlaceEntity, MoveEntity, RemoveEntity) — return errors. The adapter is read-only because chain handlers should never mutate the room from inside an attack chain. If they need to (knockback, push), that mutation belongs on the orchestrator side AFTER the chain completes, with proper event emission.\n\nWhy a read-only adapter (Copilot review feedback during PR #532): mutations from inside chain handlers would bypass the encounter's broker / per-viewer projection / persistence write-back. Forcing the adapter to error on mutating verbs makes that boundary explicit at the type level.\n\nWhen adding a new gamectx-consuming chain handler: use the adapter as-is. If you need a mutating spatial verb during a chain, that's a smell — file an issue and route the mutation through the orchestrator's post-chain step instead."
    }
  ]
}
