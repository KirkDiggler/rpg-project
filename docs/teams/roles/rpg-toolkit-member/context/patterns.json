{
  "items": [
    {
      "id": "pat-001",
      "summary": "ToData / LoadFromData persistence pattern",
      "details": "Every stateful toolkit component implements ToData() *XxxData and LoadFromData(ctx, *XxxData, events.EventBus) (*Xxx, error). Data struct has JSON tags, no methods. Runtime struct has no JSON tags, holds event bus subscriptions. rpg-api serializes Data to Redis; calls LoadFromData before any rule call; calls ToData after to serialize the result back. Never (nil, nil) — always return a valid object or an error. Present in: character, Draft, BasicEnvironment, RoomData.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-002",
      "summary": "ToJSON / LoadJSON for conditions and features (heterogeneous blobs)",
      "details": "When multiple runtime types share a single opaque JSON field (conditions list, features list), the pattern is: ToJSON() (json.RawMessage, error) for serialization; LoadJSON(json.RawMessage) (BehaviorType, error) for routing. LoadJSON peeks at ref.Value in the JSON, switches on it to the correct constructor, unmarshals the full struct. This keeps rpg-api's stored JSON opaque. Defined in rulebooks/dnd5e/conditions/loader.go and rulebooks/dnd5e/features/loader.go.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-003",
      "summary": "BusEffect lifecycle: Apply subscribes, Remove unsubscribes, collect all errors on Remove",
      "details": "Any game mechanic that modifies behavior through events implements BusEffect: Apply(bus EventBus) error (subscribe handlers, store IDs), Remove(bus EventBus) error (unsubscribe all, clear IDs), IsApplied() bool. Remove must collect ALL unsubscribe errors (errors.Join or rpgerr accumulation) — never stop at the first failure. PR #603 established this convention. Stopping at first error leaves orphaned subscriptions.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-004",
      "summary": "Refs package: typed constructors over raw strings",
      "details": "rulebooks/dnd5e/refs/ provides named ref constructors: refs.Features.Rage(), refs.Conditions.Dodging(), refs.CombatAbilities.Attack(), refs.Actions.Strike(). These are the canonical identifiers. Never use raw strings like 'rage' or 'dnd5e:features:rage' in switch cases or comparisons — always use refs.* or the module's typed constant. This makes identifiers greppable and refactorable.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-005",
      "summary": "Testify suite pattern — mandatory for all non-example tests",
      "details": "All tests use suite.Suite + SetupTest() + s.Run() for subtests. SetupTest() creates fresh mocks and instances before each test function. SetupSubTest() resets shared test data before each s.Run(). Example tests (example_*_test.go) may be flat — they are documentation, not behavioral tests. Mocks go in mock/ subdirectory per module, generated with go:generate using uber's gomock.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-006",
      "summary": "Input/Output types on every function — non-negotiable",
      "details": "Every function at every layer uses Input/Output struct types: func DoThing(ctx context.Context, input *DoThingInput) (*DoThingOutput, error). No positional parameters beyond ctx and input. This applies to internal helpers too. Never return (nil, nil) — always a valid object or an error. This is the workspace-wide rule enforced by CLAUDE.md.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-007",
      "summary": "LayerOrchestrator FindPath is room-to-room, not intra-room",
      "details": "BasicRoomOrchestrator.FindPath(sourceRoomID, targetRoomID) returns a sequence of room IDs to traverse. It is not an A* within a single room. For intra-room pathfinding use SimplePathFinder.FindPath(start, goal CubeCoordinate, blocked) — but this is hex-only (issue #614). Square-grid intra-room pathfinding has no toolkit solution today.",
      "added": "2026-05-02"
    },
    {
      "id": "pat-encounter-npc-verb",
      "summary": "Encounter.NPCAct(npcID) wraps monster.TakeTurn, captures dnd5e-bus events via temporary subscription, re-emits as encounter-scoped per-viewer events",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.8 playtest 2026-05-09 (toolkit encounter v0.3.0)",
      "details": "Shipped in Wave 2.8 (rpg-toolkit#633). The Encounter aggregate exposes a single NPCAct verb that the rpg-api EndTurn handler calls when initiative advances to an NPC actor:\n\n  func (e *Encounter) NPCAct(npcID core.EntityID) error\n\nThe verb's job:\n  1. Build TurnInput from encounter state (active actor's snapshot, target list within perception, action economy budget).\n  2. Subscribe a temporary listener to the dnd5e bus topics (AttackTopic, DamageTopic, ConditionTopic) for the duration of this NPC turn.\n  3. Call monster.TakeTurn(turnInput) which decides + activates the NPC's chosen action.\n  4. Capture every dnd5e bus event the NPC's action publishes during the call.\n  5. Translate each captured event into an encounter-scoped per-viewer event (DamageDealtEvent, ConditionAppliedEvent) and publish through the encounter Broker. Viewer slicing happens here; consumers see what their PerceptionView allows.\n  6. Unsubscribe the temporary listener (lifecycle scoped to the call).\n  7. Internally call EndTurn to advance initiative; the next active actor (player or NPC) is the EndTurn handler's loop's responsibility (see pat-v2-npc-turn-dispatch on the api-member side).\n\nWhy temporary subscription (not durable): the dnd5e bus is process-wide; we don't want every encounter listening to every other encounter's events. Scope the subscription to the NPCAct call only.\n\nValidated reality note (2026-05-09): the dnd5e action layer (bite, scimitar, melee, ranged) currently only publishes the cause AttackEvent without resolution data. The encounter SDK uses a STAND-IN RESOLVER inside NPCAct: d20 + AttackBonus vs target AC, damage rolled from DamageDice, nat-20 crit, nat-1 miss; uses MonsterData snapshot fields. The captured-damage / captured-condition listener paths are wired but currently no-op until rpg-toolkit#635 lands. Once #635 ships, drop the stand-in resolver and rely on the captured paths."
    },
    {
      "id": "pat-encounter-combat-event",
      "summary": "Combat events follow the per-viewer + sealed-type pattern as DoorOpenedEvent. AttackResolved is cause; DamageDealt + ConditionApplied are effects",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.8 playtest 2026-05-09 (toolkit encounter v0.3.0)",
      "details": "Shipped in Wave 2.8 (rpg-toolkit#633). Combat events live in encounter/events/ and follow the existing sealed-type + per-viewer-projection pattern established by DoorOpenedEvent / HexRevealedEvent / EntityAppearedEvent in Wave 2.7 (see encounter/events/door_opened.go as the template). New events:\n\n  - AttackResolvedEvent — cause-stage. Carries attacker, target, action ref, hit/miss/critical, attack roll, total. Publishes once per attack resolution.\n  - DamageDealtEvent — effect-stage. Carries target, amount, hp_after.{current,max}, damage type, source attacker. Publishes once per damage application.\n  - ConditionAppliedEvent — effect-stage. Carries target, condition ref, source ref, duration. Publishes once per condition application.\n  - TurnStartedEvent / TurnEndedEvent — initiative lifecycle. Carry the actor id + round.\n  - ModeChangedEvent — mode flip (FREE_ROAM ↔ TURN_BASED). Carries new mode.\n\nEvery event implements the sealed events.EncounterEvent interface and carries a PerPlayer map: each viewer's PlayerID → that viewer's slice of the event (or absence). The Broker delivers based on PerPlayer; the rpg-api translator handles per-viewer slicing on the wire (see api-member's pat-v2-combat-event-translator).\n\nCause vs effect split (validated in Wave 2.7, reused in 2.8): cause and effect are SEPARATE events with INDEPENDENT per-viewer projections. Two viewers might see different halves (stealth, line-of-sight). Don't merge them inside the toolkit, even if 'they always fire together' for the simple case — different waves will introduce viewers who see one but not the other.\n\nValidated reality note (2026-05-09): AttackResolvedEvent is implemented and ready but doesn't fire from the dnd5e action layer today (see pat-encounter-npc-verb stand-in note). Once rpg-toolkit#635 wires combat.ResolveAttack into bite/scimitar/melee/ranged, AttackResolvedEvent will start flowing through the NPC capture path.\n\nWhen adding a new combat event (HealReceived, ConditionRemoved, Knockback): copy door_opened.go's shape, define the per-viewer slice, document whether it's cause-stage or effect-stage, and add a translator case in rpg-api's translate_combat.go."
    },
    {
      "id": "pat-encounter-turn-state",
      "summary": "Mode + initiative + active-actor live on encounter.Data. SetMode rolls initiative on flip; EndTurn advances ActiveIdx; round increments on wrap",
      "added": "2026-05-09",
      "status": "validated",
      "validated_in": "Wave 2.8 playtest 2026-05-09 (toolkit encounter v0.3.0)",
      "details": "Shipped in Wave 2.8 (rpg-toolkit#633). encounter.Data grew Wave-2.8 fields:\n\n  type Data struct {\n      // ... existing Wave 2.5/2.6/2.7 fields ...\n      Mode        core.EncounterMode  // Unspecified | FreeRoam | TurnBased\n      Initiative  []core.EntityID     // ordered list of actor ids; both players + monsters\n      ActiveIdx   int                 // index into Initiative; wraps to 0 on round end\n      Round       int                 // 1-indexed; increments when ActiveIdx wraps\n      Monsters    map[EntityID]*MonsterData  // NPC actors with MonsterRef + AC + HP + AttackBonus + DamageDice + Position + ...\n  }\n\nVerbs that mutate this state:\n  - SetMode(newMode) — flip between FreeRoam and TurnBased. On a flip TO TurnBased, rolls initiative for every actor (players + monsters) and populates Initiative. On a flip TO FreeRoam, clears Initiative + ActiveIdx + Round (or preserves for retry — design choice, currently clears).\n  - EndTurn(actorID) — validates actorID matches the current ActiveActor, advances ActiveIdx; if ActiveIdx wraps past len(Initiative)-1, resets to 0 and increments Round. Publishes TurnEndedEvent for the leaving actor + TurnStartedEvent for the new active actor.\n  - NPCAct(npcID) — see pat-encounter-npc-verb. Internally calls EndTurn after the NPC's action.\n\nQueries:\n  - Mode() core.EncounterMode\n  - InitiativeOrder() []core.EntityID (defensive copy)\n  - ActiveActor() core.EntityID — returns Initiative[ActiveIdx] or empty if out of range.\n  - Round() int\n\nPersistence: ToData() and LoadFromData() round-trip all five new fields. Mode is serialized as int (its enum representation). MonsterData uses the same JSON shape as PlayerData — see pat-001 (ToData / LoadFromData persistence pattern) for the convention.\n\nWhen extending: new turn-related state (action economy on the active actor's turn, reaction window state, delay/ready actions) lives on encounter.Data alongside these fields. New verbs that mutate turn state go on the Encounter aggregate, not on Monster or Player — keep one source of truth for the initiative cycle."
    }
  ]
}
