import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function piTeamHarnessSkeleton(_pi: ExtensionAPI): void {
  // PIH-1 intentionally provides only loadable module boundaries.
}
