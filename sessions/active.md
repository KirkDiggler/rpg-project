# Active handoff — 2026-08-13

## Now

Wave 0 is LIVE VERIFIED. Wave 1 (#180) is exploratory semantic-scope work; implementation has not started.

## Solid

- Wave 0 delivered the Go proto contract as generated root `v0.1.120` / `ba7eda1f1833`, toolkit `encounter/v0.49.1`, and rpg-api#771; verification is recorded on [#192](https://github.com/KirkDiggler/rpg-project/issues/192#issuecomment-5206548237).
- Wave 1 runs structurally usable scope graphs without archetype cardinality/content/spawn enforcement. Missing semantic conventions are follow-up findings only where an existing surface permits; no diagnostics proto/API is added.
- rpg-api-protos#210 is a separate TypeScript/GitHub Release publication defect, not a Go gate. #772 (API update durability) and #885 (toolkit documentation) are separate.

## Next

**[PR #200](https://github.com/KirkDiggler/rpg-project/pull/200) merged 2026-08-06** — the consistency review this line used to ask for is done; the course correction landed. Wave 1 implementation issues for #180 can be cut when that lane is picked back up.

**Active work is the session SDK lane** (see "Lane: encounter reset" below): W0/W1/W2 shipped, and **W3 step 1 (characters load) shipped 2026-08-13 as `session/v0.3.0`** (#949 + triplet #947 both merged). **Next: the rest of W3** — NPCs in `SessionData`, and conditions surviving a suspension, which is the invariant W2 forced. Tracking [#945](https://github.com/KirkDiggler/rpg-toolkit/issues/945).

## Pointers

- [#180](https://github.com/KirkDiggler/rpg-project/issues/180) · [PR #200](https://github.com/KirkDiggler/rpg-project/pull/200) · [#192 verification](https://github.com/KirkDiggler/rpg-project/issues/192#issuecomment-5206548237) · [rpg-api-protos#210](https://github.com/KirkDiggler/rpg-api-protos/issues/210)

## Lane: encounter reset (play/ family)

**Shipped through 2026-08-11:** `play/{clock,intel,record,interrupt}` all v0.1.0 merged+tagged (docs PRs #906/#911/#912 merged as ratification; journey 052 = bus keeps observation, discovery moves to enumeration). Spatial maintenance shipped by Platform (`tools/spatial/v0.9.0`). **The composition is live: `rulebooks/dnd5e/encounter` v0.1.0** (toolkit #921 merged+tagged 2026-08-11; triplet #918 merged; own module, laws C1–C8, tomb-watch scene, verified-transcript pattern + freeroam workbench + generic `scripts/verify.sh`).

**Wave 2 SHIPPED 2026-08-11:** toolkit [PR #924](https://github.com/KirkDiggler/rpg-toolkit/pull/924) merged + tagged **`rulebooks/dnd5e/encounter/v0.2.0`** (tag verified → b51d0c0); triplet #923 merged as ratification. Platform notified on rpg-api#793 — their dependency signal is green; wiring can proceed. Post-build pre-tag rounds all absorbed: Copilot (grid persists as string; endpoint presence required), Kirk's axial catch (hex = AxialHexGrid, cube math, identity projection), Platform review (nil decider reattachment holds; integral axial at every hex seam — semantic-owner fix filed as toolkit#926 for spatial ingress). Final: 288 tests, five review rounds. Delivers: connection endpoints (nine-defect validation both seams), per-room `GridShape` (hex first-class — answers Platform's square-grid pushback; spatial's bounded HexGrid validity ≡ square's, gridless is the discriminating signal), the `Traverse` verb (TransitionEntity+PlaceEntity, limbo window proven unreachable under four documented invariants), `Decide(Snapshot{Room,Position,Holdings})` + `IntentTraverse` (pursuit through doorways; found+fixed a REAL Pump aliasing bug — planned list held member value copies), the vault-chase scene, `Example_theTraverse`, two-room workbench. 259 tests; 2 sonnet + 2 Opus review rounds, ~50 mutants/probes; `gorelease` verdict = v0.2.0 exactly (one ratified breaking change: Decider interface). Triplet [PR #923](https://github.com/KirkDiggler/rpg-toolkit/pull/923) open with full execution addenda — merges as ratification. Newly pinned laws: full-tick-then-evaluate closure; ending eval is decision-order across actions / declaration-order within one; reject-never-crash covers reentrant deciders. Follow-up noted (not filed): intel `Refreshed` delta ordering nondeterministic (never observable in blobs/transcripts).

**Wave 3 SHIPPED 2026-08-12 (world anchoring):** [PR #932](https://github.com/KirkDiggler/rpg-toolkit/pull/932) merged, tag **`rulebooks/dnd5e/encounter/v0.3.0`** verified → **f2ac33a**; triplet [#930](https://github.com/KirkDiggler/rpg-toolkit/pull/930) merged as ratification. Platform notified on rpg-api#793 — map surface live, with the Locate→Move trap called out explicitly. CI was green incl. the `gorelease-dnd5e-encounter` compat gate; `gorelease -base=v0.2.0` → **v0.3.0** exactly. 17 commits, **599 tests**, five build tasks + a final gate + three closing rounds; reviews: 3 sonnet + 4 Opus. Triplet [#930](https://github.com/KirkDiggler/rpg-toolkit/pull/930) carries the complete execution record and merges as ratification. Delivered: W1–W5 enforced identically at both seams (Setup and Load share ONE validator — a single mutation kills pins on both, which is the structural proof they cannot drift), `Atlas()` (rooms with absolute cells/occluders/boundaries + doorway kissing pairs; construction-truth; cache it — ~128MB at the field budget), the `Absolute()`/`Locate()` bridges, and the absolute-continuity scene (a 14-row projected chase, doorway step at distance exactly 1; its negative control is unconstructible through the public API *because* W3 rejects it). Defects caught pre-merge that would have reached the game: two wrong-verdict integer overflows through the public API (origins past 2^63 rejecting disjoint rooms; unbounded dims accepting overlapping ones), a panic from a few hundred bytes of persisted data (2^30 room → 2^60 allocation in `Atlas`), W2 unsound under fractional square origins (~81% interpenetration with disjoint cell sets), occluder entity-ID collisions wrongly rejecting legal fields, and a workbench that had been dead on startup since T1 (nothing in CI ran the binary). New family standard minted: **over-tightening defense** — a rejection table proves a validator rejects; only a rich positive control proves it doesn't over-reach (four plausible "hardening" changes would have passed CI while breaking every hex encounter with a negative-axial objective). Follow-ups filed, deliberately not rushed into the tag: [#933](https://github.com/KirkDiggler/rpg-toolkit/issues/933) (host projection ergonomics: the Locate→Move trap, Members() without positions), [#934](https://github.com/KirkDiggler/rpg-toolkit/issues/934) (two validation asymmetries). **Next: toolkit [#916](https://github.com/KirkDiggler/rpg-toolkit/issues/916)** — the resolution axis (suspendable MOVEMENT + OA-with-option via play/interrupt, design-first), with the six-scene use-case ladder recorded below as its acceptance spine. rpg-api#793 wiring is Platform's whenever they pick it up; nothing blocks it.

**PIVOT 2026-08-12 — the session SDK ([toolkit#935](https://github.com/KirkDiggler/rpg-toolkit/issues/935), docs [PR #936](https://github.com/KirkDiggler/rpg-toolkit/pull/936), open for Kirk's review).** Scoping #916 turned into something larger once we asked who the customer is. Four findings redirected it: (1) `encounter.Move` is a single **hop**, not a walk — an OA is a property of the path, but the walker need not be the encounter; (2) **walls were already real** (`spatial.Boundary` carries separate BlocksMovement/BlocksLineOfSight, registered by the composition, projected by Atlas) so a doorway is just an *unblocked crossing* — which means **T3 "sight never crosses a connection" is not geometry**, it's a room-scoped shortcut anchoring made obsolete (retiring it = a perception wave computing field-level LOS over Atlas, the only thing that sees every room at once; spatial can't own it, its boundaries are registered *within* a room and a doorway spans two); (3) **`Member` is `{ID, Kind, Room}`** — no HP, sheet, conditions, or attack, so **#916's headline cannot be built in the composition**: the window opens, the player answers "I swing," and there's nothing to swing with; (4) the customer's loop (load encounter+monsters+players → load into the encounter → act → get data back → save) is missing step two entirely. **Shape (Kirk-ruled):** the encounter stays *the world* (geometry/placement/perception/story/endings, aggregate discipline intact); a `rulebooks/dnd5e/session` manager above it is *the table* (entity roster, bus, resolution, interrupt custody) — composer of composers, AWS-SDK style. Game server implements repository ports trading in `Data` types and **never holds a domain object**; stateless per call (we're turn-based). Suspension reaches the customer as one thing: a verb returns `Pending` instead of done, `Answer` resumes it. **Strategy:** wrap what exists, migrate rpg-api once, replace behind it — the falsifiable claim being *after the migration wave, no subsequent wave changes an rpg-api source file*, gated by `gorelease` from the first tag. **Discipline that makes it a boundary and not a rename:** the acceptance scenes were written *before anyone opened the old code*. Also settled: three mechanisms, not one — **chain** for modification during resolution (rage's bonus damage), **bus** for observation (was I hit / turn ended / the curse watching for a priest), **checkpoint** (enumerated, suspension returned as a value) as the only place a pause is born; journey 052's rule was never "no bus", it was *control flow never rides the bus*. The trap is **not** an interrupt — it does its thing; a checkpoint's four outcomes are do-nothing / halt / resolve / ask, and only the last suspends. **DESIGN APPROVED by Kirk 2026-08-12**; `plan.md` added — six waves, cut by the rule *anything that shapes a public type goes in wave 1, anything purely internal waits* (under the gorelease promise the wave with the most trivial code carries the most irreversible decisions). **W0** encounter log retention (parallel, different module → `encounter/v0.4.0`); **W1** shell + free-roam verbs + event stream → `session/v0.1.0`; **W2** the interrupt spine → `session/v0.2.0`; **W3** entities on the bus → `v0.3.0`; **W4** combat, where rpg-api migrates → `v0.4.0`; **W5** reactions, closing #916 → `v0.5.0`. **W2 needs no combat** — the headline scene (Alice walks, ogre comes into view at step 3, world freezes, different process answers, she resumes) is entirely reachable with the composition's existing sight refresh, so the interesting thing lands second not fifth. **The most valuable wave-1 test is a boundary test, not a behavior test**: it fails if any exported signature references a type from encounter/combat/clock/intel/record/interrupt — that's what makes "rpg-api only bumps a version" mechanical rather than aspirational, and the plan records that if it proves unwritable the strategy needs rethinking rather than a workaround. Grounded survey: **rpg-api imports ZERO of the composition today** (verified — no reference anywhere), so W1 costs it nothing it has; its **22 files / ~6,700 lines** of old-stack combat orchestration (hydrate_players, character_resolver, combatant, turn_state_project, dnd5e_combat_resolver_phased, reaction_resume) migrate at W4 and are treated as *evidence about what a game server needs, never a specification to port*. Also ruled in-session: NPCs are session-scoped in `SessionData` with no repo until a durable NPC exists (asymmetric reversibility — adding a Config port later is compatible, removing one isn't); delete-on-death rejected (loses the corpse; players loot bodies); log retention configurable with an **extremely small default** because that makes full-resync the common path and keeps it exercised. **W0 SHIPPED 2026-08-12:** [PR #939](https://github.com/KirkDiggler/rpg-toolkit/pull/939) merged → **290d6d6**, tag `rulebooks/dnd5e/encounter/v0.4.0` verified against it. Story log bounded (`SetupInput.Retention`, persisted; `DefaultRetention`=32, `RetentionUnbounded`=-1) + `ErrTrimmed` rejecting unhonorable resume points, with `AfterSeq==0` exempt because zero means "I hold nothing" — the first-load path that trimming must never break. All 8 append sites route through one `appendBeat` seam. 6 mutants/6 kills. Copilot found 3 real things (trim guard fired at reach-not-exceed — **verified unobservable**, mutating the fix back out failed no test, fixed for correctness-of-intent only; `AfterSeq` documented as the inclusive-bound misnomer it is, rename deferred as breaking).

**W1 SHIPPED 2026-08-12 — [PR #942](https://github.com/KirkDiggler/rpg-toolkit/pull/942) merged → **407bd57**, tag `rulebooks/dnd5e/session/v0.1.0` verified against it.** `rulebooks/dnd5e/session` v0.1.0: 10 verbs (StartSession/Join/Move/Traverse/Exit/End/Atlas/Status/View/Story), 89 tests, `verify.sh` clean, `-race` clean, `gorelease`→v0.1.0. Direct deps: **one** (`encounter v0.4.0`). Files: types.go (wire) / convert.go (the converter layer) / data.go (persistence) / read.go / write.go / move.go / events.go / ports.go, + `cmd/session-workbench`. **The load-bearing test is `TestNoInnerTypeCrossesTheBoundary`** — parses the package's own AST, fails on any toolkit type reachable from an exported decl; verified against a REAL leak (temp `record.Entry` field caught by file/line/field/type), carries a meta-pin, allow-list has 2 entries each with a written reason. **Its blind spot, found and closed:** sentinels aren't types-in-signatures, so `encounter.ErrTrimmed` could have become host contract invisibly — composition sentinels are translated and the test asserts the inner one is NOT reachable (killer mutant: `fmt.Errorf("%w: %w", ErrStoryTrimmed, err)` looks *more* informative and still leaks). **Converter safety net:** `convert_test.go` requires every inner field carried or justified-with-reason, + a stale-entry guard; proven by dropping `Sighting.CurrentVia` (the `Occluders` attempt failed as a *build* error, so I retried against a field nothing else asserts). Rulings applied throughout: write world-then-session (partial failure leaves a collectable orphan, never a session pointing at nothing); path adjacency enforced NOW (loosening is compatible, tightening isn't) via spatial's own grid (axial (1,1) is cube-2 / Chebyshev-1 — a defect class already shipped once); statelessness pinned by two managers interleaving joins; concurrency doors documented on the ports (pessimistic = optional capability interface, NEVER a method on `SessionRepository`; optimistic = free given Kirk's checksum-CAS scheme). **CI gap found and fixed:** `compat.yml` enumerates gorelease jobs per module by hand, so #942 initially passed with NO compat gate; added `gorelease-dnd5e-session`. The enumeration **fails open** — a job deriving modules from go.mod files would gate the next new module by default rather than by memory (noted, not done).

**Composition follow-ups the SDK work surfaced (all filed, none blocking):** [#940](https://github.com/KirkDiggler/rpg-toolkit/issues/940) every beat is addressed to EVERY member — `record` has per-viewer filtering, `intel` models perception, the composition never connects them; harmless as a query, a fog-of-war leak as a push channel (scene 7 corrected on #936 to say so); [#941](https://github.com/KirkDiggler/rpg-toolkit/issues/941) tags coarser than beats (`membership` = join+exit), so event kind is read from the payload today — the ONE place the SDK interprets a body it doesn't own, and it fails *silently* if the payload shape changes; [#933](https://github.com/KirkDiggler/rpg-toolkit/issues/933) `Members()` reports room but not position, forcing a snapshot per walk. Plus: retention default → 8 (Kirk: "either is fine", but his reason — seeing the effect — points to 8, with transcript scenes opting into `RetentionUnbounded`). **In every case the layering holds: fixing it in the composition fixes the SDK for free.**

**Copilot round on #942 (all 3 legitimate, fixed):** `(nil,nil)` from GetSession was read as "session exists" — a contract violation, not an answer, and BOTH guesses cost something (misleading error vs. overwriting a live session); now `ErrBadRepository`, unified with the read path which already treated it as a repo bug. The other two shared a root cause worth remembering: **the package docs were written from the DESIGN rather than the CODE** — `doc.go`'s snippet used `out.Pending` (doesn't exist; a reader copying it gets a compile error) and `ports.go` promised hosts would persist interrupt windows when `SessionData` holds two strings. `doc.go` now states which laws are in force and which two (S5/S7) are commitments.

**Docs added (Kirk-directed):** session `README.md` (charter ONLY — what it's for, what it's not, file roles), `docs/architecture/components/rulebook-dnd5e-session.md`, and a `docs/quality.md` entry graded **A-** with what would raise it *judged from what was available when built*: no real consumer migrated (verb set unproven), statelessness asserted not benchmarked, `kindOf` interprets a payload until #941, partial-save path thin until entities bring a second writer — and explicitly NOT counting #940/#933 against it, since those are the composition's. **Doc-ownership split (Kirk):** each code repo owns its own status/quality/architecture; rpg-project says how we *work*, not what was done — except `docs/ideas`, which records the plan as it stood at the time.

**[rpg-project#218](https://github.com/KirkDiggler/rpg-project/issues/218) filed:** every hand-maintained module list in the toolkit **fails open** — 3 instances found in one afternoon (compat.yml missing the session gate so #942 initially passed with NO compat check; 7/23 modules lacking READMEs, exactly the newest work; status.md+quality.md untouched since before the play/ family and encounter v0.2–v0.4 existed). Proposal: one CI job deriving the module list from go.mod so the NEXT module is covered by default, plus writing the conventions down before pointing a review bot at them. Governing rule to encode: **stable claims in hand-written docs, volatile claims derived or tested.**

**Two late corrections Kirk made pre-merge, both while renaming was still free (nothing tagged):** (1) **"ports" is gone** — the word collides with TCP ports and platform porting, and the adapter half of the metaphor is wrong outright (an adapter implies a third thing between two parties; the host just writes the implementation). Two of the three ARE repositories; `EventStream` is outbound delivery, not storage, so it moved to `events.go` beside the fan-out. `ports.go`→`repositories.go`, `ErrMissingPort`→`ErrIncompleteConfig` (named for the condition, since "missing repository" is only accurate while every required dep happens to be one). What survives is the one idea worth naming: these interfaces point **outward** — the SDK calls, the host implements. (2) **The event stream is REQUIRED, not optional** — "multiplayer fan-out" was mis-teaching: a verb's response describes the CALLER's own action, and monsters acting / the clock advancing / another member crossing a doorway appear in nobody's return value. Single-player needs it for the same reason. Two claims separated that I'd conflated: the stream is *required to wire* and *allowed to fail* (S10 untouched). `DiscardEvents` keeps genuine silence possible as a **stated** decision — a nil reads as oversight, an explicit discard greps. Direction matters: required→optional is a compatible loosening; optional→required breaks every host, so starting required is the only reversible choice.

**#936 MERGED 2026-08-13** → b6582d8, after Kirk caught that the docs still said "ports" and still described the event stream as optional — neither of which is what shipped. Fixed with the reasoning recorded rather than a silent rename (adapter is wrong because nothing sits *between* the parties; the stream is required because a verb's response can't carry what happened to anyone else, and requiring it now is free while requiring it at v0.3.0 would break every host that took the option). Three design-vs-shipped drifts fixed too: `Characters` still in the Config literal, `SessionData` shown with fields v0.1.0 lacked, waves 0/1 unmarked. **Rule settled for the triplet's whole life:** ratified sections get annotated with the reason they changed; plan sections get rewritten freely; the status line marks the boundary and advances one wave at a time. And the reason to merge rather than hold to W5 — co-location, not tidiness: the drift existed *because* the doc lived on a different branch from the code, so from here each wave's implementation PR amends it in the same commit that makes the amendment true.

**W2 SHIPPED 2026-08-13 — [PR #944](https://github.com/KirkDiggler/rpg-toolkit/pull/944) merged → **556b2e1**, tag `rulebooks/dnd5e/session/v0.2.0` verified against it; [#943](https://github.com/KirkDiggler/rpg-toolkit/issues/943) closed.** `gorelease -base v0.1.0` → **v0.2.0 exactly**, all additions. 120 tests, `-race` clean, `verify.sh` clean, lint clean, no nolint. **15 mutants, all killed.** The spine works end to end: Alice walks, the ogre comes into view mid-path, the world freezes, both stores round-trip through JSON into a fresh Manager, and she resumes — no combat anywhere in it.

**`play/interrupt` v0.1.0 already was the custody module**, so this wave was wiring and phase discipline, not invention: Pose/Answer/PendingFor/LoadLedger with full R9 validation, and it never interprets an option or a payload byte. Delivered: the walk as a **re-enterable phase machine** (explicit index + path in a serializable value — a straight-line loop could only suspend by parking a goroutine, which dies with the process); `Answer`/`Pending` verbs; SDK-owned `Pending`/`Option`/`Prompt`/`OptionKind`; the freeze enforced **structurally** (`openForChange` refuses, `openForWrite` doesn't — `Answer` needs a frozen session, so forgetting the freeze means actively choosing the wrong opener) with a typed `*FrozenError` naming the window and its audience; `interrupt.LedgerData` in `SessionData` and on the boundary allow list as a stored shape **only** — never `interrupt.Window`/`Option` in a signature.

**Rulings made and pinned:** no window on the final cell (continue and stop would be the same act); `Prompt` carries the moment never the mechanism (a test asserts it has no field naming the checkpoint — a `Reason` field would become what clients switch on and could then never change); sighted subjects sorted not iteration-ordered (C8); stopping is not undoing. **Write order is now a correctness argument, not a tidiness one:** encounter first / session second, because world-without-window is a stoppage where every persisted fact is true while window-without-world resumes past cells nobody walked — corruption that looks like progress. **S6 finally exercised** — a suspending walk writes two aggregates, a non-suspending one writes one, both pinned.

**Two things worth carrying forward.** (1) **A mutant survived the first pass and it mattered:** the resume test asserted the right cell was walked, which stayed true with re-validation aimed at the whole path instead of the unwalked suffix, because the walk is driven by the phase index. A pin that didn't pin; replaced by two tests (tampered suffix → rejected; tampered *already-walked prefix* → still resumes, because those cells are behind her). (2) **quality.md holds at A- and says why it didn't rise** — the thin partial-save path closed, and a new reservation opened: *"stop the walk when the walker sees something new"* is a game rule living in a module whose charter says it owns no rules. It's there because **no module owns *when* a resolution should pause** — perception owns what is seen, not what that should interrupt. Named as squatting, moves when the second checkpoint kind makes the deciding thing's shape visible; inventing that owner from one example would be guessing.

Also: CI's modification check caught `go.mod` drift (`core` is direct now — `core.EntityID` is named at the interrupt seam; the local tidy predated that import), which also falsified quality.md's "One direct dependency" line — corrected to the real list with a note that `core` is held off the boundary.

**Copilot round on #944 (1 finding, legitimate, and it falsified a claim I'd made):** the `SaveReport` was computed in `persist` and **dropped by every verb**, because a verb returns no output when it returns an error — so S6 named the pieces to nobody. Survivable while `StartSession` was the only two-aggregate writer; not once windows make a partial save reachable on an ordinary walk. Fixed with a typed `*SaveError` (errors.Is → `ErrSaveFailed`, errors.As → the report, `Unwrap` returns the sentinel AND the store's error), matching `*FrozenError`'s shape and keeping the convention that an error means no output. 4 more mutants killed → **19 total**. Lesson worth keeping: *computing the evidence is not the same as delivering it* — the v0.1.0 code had the identical gap and no test could see it, because nothing yet produced a partial save.

**W3 STEP 1 SHIPPED 2026-08-13 — [PR #949](https://github.com/KirkDiggler/rpg-toolkit/pull/949) merged → **5c79152**, tag `rulebooks/dnd5e/session/v0.3.0` verified against it; design triplet [PR #947](https://github.com/KirkDiggler/rpg-toolkit/pull/947) merged → 9e84562 as ratification.** **The repo AUTO-TAGS on merge to main** (`auto-tag-modules-safe.yml`; the other two tag workflows are disabled) — bump derived from conventional-commit prefixes, so a `feat:` takes the minor the moment it lands. That means **a wave's version is decided by its FIRST merge, not its last**, and the plan's tag column (which assumed one merge per wave) was corrected rather than worked around: it now stops predicting after W3. Nothing real depended on the numbering — semver tracks API change, not milestones, and the falsifiable claim is about *which* wave migrates (4, combat), not its number. So W3 runs from v0.3.0 and its remaining step will take v0.4.0. **Copilot round on #949 (1 finding, legitimate, and it exposed two more of my own overclaims):** `projectCharacter` read HP/AC via `ch.ToData()` — a serialisation, not a getter (clones maps, marshals every feature/condition to JSON, stamps `UpdatedAt: time.Now()`, making a read path non-deterministic); fixed to accessors, behaviour-identical, measured at ~1.0µs/489B saved on a condition-free fixture (the floor, since `ToData` marshals each condition). The exposed doc errors a host would have believed: `CharacterState` claimed every field was derived and that AC accounted for equipment (`AC()` returns the stored field), and `Speed` was documented as accounting for conditions (`GetSpeed` returns **base** speed from race; modifiers ride the movement chain). **Benchmark pulled forward and answers the wave's named risk:** player vs monster join differ by exactly the character load — **~5.1µs and 64 allocs**, ~30µs at party scale, so stateless-per-call does NOT need a cache and the caching-repository escape hatch stays theoretical.

**(superseded, kept for the build record) W3 STEP 1 BUILT 2026-08-13 — [PR #949](https://github.com/KirkDiggler/rpg-toolkit/pull/949).** Kirk scoped the wave: *"our goal for this wave is to get the character loaded. if we want to take a step or 2 to get there that is ok"* — and approved the design otherwise. Step 1 delivers the load itself: `CharacterRepository` on `Config`, `Join` loads the sheet for `KindPlayer` via `Member.Kind` routing, `JoinOutput.Character` reports derived state. **Speed is the proof — it is not a field of `character.Data` at all**, so a dwarf's 25 can only come from a reconstituted sheet; the control test pins that two characters differing only in race differ in derived speed (human 30, which is also `GetSpeed`'s fallback). Allow-list split into the two categories with a disjointness pin. **136 tests, 9 mutants all killed, lint 0, no `nolint`, `gorelease` → v0.3.0 all additions, verify.sh clean incl. transcripts.** **Two pins corrected after mutation testing found they claimed more than they proved** — neither was a code defect, both were comments asserting a guarantee the test didn't give: the load-vs-placement *ordering* pin (load-act-save already discards the in-memory encounter on error, so order isn't load-bearing; reframed to guard the failure aborting the verb) and the *no-caching* pin (only one verb loads a character and each row joins a different member, so a per-ID cache changes nothing observable — the caching mutant survived, which was the honest signal). **Live demonstration of the documented blind spot: `gorelease` reported `Config.Characters: added` as a COMPATIBLE change.** New upstream finding filed as **[#948](https://github.com/KirkDiggler/rpg-toolkit/issues/948)** — `character.LoadFromData` swallows every condition-load error (logs and continues), so a corrupt blob vanishes silently and the next `ToData()` persists the loss; pinned not fixed, since the policy belongs to the character package. Remaining for W3: NPCs in `SessionData`, conditions across a suspension, and the load+attach benchmark. **Design pass context below —**

**W3 DESIGN PASS OPEN 2026-08-13 — [#945](https://github.com/KirkDiggler/rpg-toolkit/issues/945), [PR #947](https://github.com/KirkDiggler/rpg-toolkit/pull/947), design approved by Kirk; stays open through implementation, merges as ratification.** Correction to what this handoff said before: **W3 does NOT bring the second checkpoint kind** — the plan is explicit that its bus carries observation only and *nothing in this wave can suspend*. The pause-policy question stays parked until W4 (combat). W3 is still the right next wave, but because W4 is the payoff wave (rpg-api migrates, the version-bump promise starts) and needs it. Design settled with Kirk before cutting tasks: **the seam takes IDs, not characters** — `character.Data` appears only on `CharacterRepository`, never in a verb input, and `*character.Character` never crosses; **contract types vs persistence shapes** are two different promises the allow-list had been blurring (*replaceable underneath* vs *a domain noun whose change we announce*) and `character.Data` is the former, beside `spatial.Position` — Kirk's framing: a character is a thing, not an implementation detail we'd refactor without telling the API; **`character.Cleanup` must not be called** in the load/act/save loop (it nils the conditions `ToData` serializes → persists a character with zero conditions, silently; safe to skip because conditions intercept on the bus rather than mutating fields, and rpg-api already does exactly this); **durable condition state must round-trip through the blob**, forced by W2's no-stack-across-a-wait rule rather than chosen. **W3 is also when the event bus enters the composition at all** — neither `encounter` nor `session` uses `events.EventBus` today (indirect only). Benchmark of load+attach at party scale is a W3 deliverable, not a maybe (Kirk: "this is the cost of our condition decoupling… benchmarking it could be a good thing"). **Found while writing it up: `gorelease` has a blind spot** — it calls a new `Config` field compatible, but a *required* one compiles everywhere and fails every existing host at construction (S8). Free now (rpg-api has not adopted the SDK; nothing is on v0.2.0), a silent break wearing a green check after W4 — hence the new discipline *every required `Config` field lands at or before W4*. Verified rather than assumed: dnd5e root **builds and passes all 25 test packages at `spatial v0.9.0` + `core v0.11.0`**, so MVS linking the modules is safe (go.mod restored; unchanged in the PR). Also filed: **[#946](https://github.com/KirkDiggler/rpg-toolkit/issues/946)** — character as its own module with its own version line (direction only, blocks nothing; note `character/data.go` saw 2 commits in 90 days vs 18 for the package, so the persisted shape is already the stable part). `CLAUDE.md`'s "NO go.work / NO LOCAL REPLACE" rule corrected in the same PR — Kirk: local overrides are how outside-in/merge-inside-out works; the banned thing is *committing* them. Open, non-blocking: **#916** needs no action — the plan lands it at W5 (opportunity attack) and its title already says so. Retention default → 8 (fold into the next encounter touch, not a standalone PR). Three composition follow-ups (#933/#940/#941) still fix the SDK for free when done.

**Wave 3 design (2026-08-11, ratified by the build):** Kirk-directed after the dungeon-map question — rpg-api's `internal/components/dungeon` coordinate prototype (`LocalPosition`/`AbsolutePosition`, origins-map `Module`; rpg-api#471, graduation planned in #479) graduates into the composition. Toolkit [#929](https://github.com/KirkDiggler/rpg-toolkit/issues/929) + triplet [PR #930](https://github.com/KirkDiggler/rpg-toolkit/pull/930) open, **awaiting Kirk review**. Laws: W1 one geometry per field; W2 rooms never overlap (subsumes origins-required — defaulted multi-room origins collide and reject); W3 doorways kiss (endpoints project to adjacent absolute cells — traversal is one world-space step, the client never learns rooms exist); W4 projection is a read (`Atlas()` static map + one `Absolute()` bridge; rules stay room-local, T3/C2 untouched); W5 anchors are construction data (presence-required at load, endpoint precedent). Compat explicitly unconstrained (Kirk: "we are holding dev until we come out the other side"). Acceptance headline: the absolute-continuity scene — the vault chase projected through the bridge is a continuous world-space path including the doorway step. Open questions on the PR: bridge-only vs enriching View; gridless (0,1] doorway bound. Walls RESOLVED (Kirk): explicit authored segments (start/end) in the dungeon YAML — content via the authoring lane, never derived, composition-invisible in v0.3. Old-stack bug toolkit#928 (dungeonspec offset→cube distance-2 under spatial v0.9) closed as superseded — class unrepresentable in the new design; durable clause (one documented offset→axial ingress) redirected to the authoring compiler; live tomb unaffected (prod main predates the v0.9 bump).

**Wiring status (corrected 2026-08-11):** Platform is NOT actively wiring rpg-api [#793](https://github.com/KirkDiggler/rpg-api/issues/793) — they surfaced a toolkit take instead and Kirk redirected the toolkit work here. Direction comment posted on #793: wiring targets v0.3 for the map surface; the prototype component deletes when wiring lands; the v0.2.0 GREEN signal stands for everything non-map (traversal, deciders, blob load). Kirk's earlier target refinement stands: acceptance = the LOCAL dev loop, `main` frozen, no doors, no dungeon-builder coupling, minimal direct spec.

**Next:** build #929 once Kirk approves the triplet (family pipeline, `feat/929-encounter-anchoring`, tag v0.3.0). Then the resolution axis, toolkit [#916](https://github.com/KirkDiggler/rpg-toolkit/issues/916) — state assessed 2026-08-11: fully unblocked, all deps tagged; six-scene use-case ladder proposed in-session (walk-away auto-OA / ask-the-human with process-restart resume / decline / two-threatener gauntlet / snake-path reaction economy / fatal answer) as the design's acceptance spine; two open design questions (phase machine lives standalone, consumed by the combat wave; traversal-provokes declared out of scope but placed). Combat wave rides it. Monster loop: buddy's damage proposal is now toolkit#927 (per-pool crit + save-gated damage) — combat lane; watch for collision with #916 wave 2 (suspendable attack phases).

Deep record: toolkit `docs/ideas/encounter-transitions/` (the wave-2 triplet + addenda), `docs/ideas/encounter/`, `docs/journey/051+052`.
